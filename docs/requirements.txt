# Documentation build dependencies.
#   pip install -r docs/requirements.txt
#   mkdocs serve
#
# mkdocstrings imports noodl to read its docstrings, so the package itself must
# be installed too (`pip install -e .`) for the API reference to build.
mkdocs==1.6.1
mkdocs-material==9.5.49
mkdocstrings[python]==0.27.0
